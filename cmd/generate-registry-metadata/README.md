# generate-registry-metadata

Generates metadata files for container image registries.

## Overview

This tool generates metadata files that describe container images in registries, including image tags, digests, and other metadata.

## Usage

```bash
generate-registry-metadata [options]
```

## Related

- Container registry management
- Image metadata

