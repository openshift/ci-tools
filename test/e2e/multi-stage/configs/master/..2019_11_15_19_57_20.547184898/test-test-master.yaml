base_images:
  os:
    name: centos
    namespace: openshift
    tag: '7'
resources:
  '*':
    limits:
      cpu: 500m
    requests:
      cpu: 10m
tests:
  - as: success
    commands: exit 0
    container:
      from: os
      # We use a dummy repo that doesn't exist, so cloning can not succeed
      clone: false
zz_generated_metadata:
  branch: master
  org: test
  repo: test
