base_images:
  os:
    name: centos
    namespace: openshift
    tag: '7'
resources:
  '*':
    limits:
      cpu: 500m
    requests:
      cpu: 10m
tests:
- as: success
  commands: exit 0
  container:
    from: os
- as: failure
  commands: exit 1
  container:
    from: os
- as: cloning
  command: ls -l && pwd
  container:
    from_image:
      namespace: ci
      name: ci-operator
      tag: latest
zz_generated_metadata:
  branch: master
  org: test
  repo: test
