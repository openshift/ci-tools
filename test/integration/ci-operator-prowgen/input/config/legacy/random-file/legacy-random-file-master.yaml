resources:
  '*':
    requests:
      cpu: "1"
tests:
- as: generated
  commands: commands
  container:
    from: root
  cron: '* * * * *'
zz_generated_metadata:
  branch: master
  org: legacy
  repo: random-file
